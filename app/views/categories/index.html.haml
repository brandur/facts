%ul.categories
  - @categories.each do |category|
    %li.category
      %h2= category.name
      = link_to "Permalink", category, :class => [ :tool, :permalink ]
      = render :partial => 'category', :locals => { :category => category }
